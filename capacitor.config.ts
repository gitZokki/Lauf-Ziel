import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'de.zokki.target',
	appName: 'Lauf Ziel',
	webDir: 'www',
	bundledWebRuntime: false,
};

export default config;
